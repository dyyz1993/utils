import { expect } from "chai";
import getRate from "../../src/string/getRate";
describe("getRate", async () => {
  it(`getRate success`, async () => {});

  it(`getRate throw`, async () => {});

  it(`getRate fail`, async () => {});
});
