import { expect } from "chai";
import formatMoney from "../../src/math/formatMoney";
describe("formatMoney", async () => {
  it(`formatMoney success`, async () => {});

  it(`formatMoney throw`, async () => {});

  it(`formatMoney fail`, async () => {});
});
