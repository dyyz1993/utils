import { expect } from "chai";
import treeNode from "../../src/base/treeNode";
describe("treeNode", async () => {
  it(`treeNode success`, async () => {});

  it(`treeNode throw`, async () => {});

  it(`treeNode fail`, async () => {});
});
