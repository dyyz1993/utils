import { expect } from "chai";
import parserComment from "../../src/ast/parserComment";
describe("parserComment", async () => {
  it(`parserComment success`, async () => {});

  it(`parserComment throw`, async () => {});

  it(`parserComment fail`, async () => {});
});
