import { expect } from "chai";
import merge from "../../src/object/merge";
describe("merge", async () => {
  it(`merge success`, async () => {});

  it(`merge throw`, async () => {});

  it(`merge fail`, async () => {});
});
