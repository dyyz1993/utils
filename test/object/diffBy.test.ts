import { expect } from "chai";
import diffBy from "../../src/object/diffBy";
describe("diffBy", async () => {
  it(`diffBy success`, async () => {});

  it(`diffBy throw`, async () => {});

  it(`diffBy fail`, async () => {});
});
