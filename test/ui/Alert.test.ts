import { expect } from "chai";
import Alert from "../../src/ui/Alert";
describe("Alert", async () => {
  it(`Alert success`, async () => {});

  it(`Alert throw`, async () => {});

  it(`Alert fail`, async () => {});
});
